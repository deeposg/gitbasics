<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Location_Address__c</fullName>
    <description>Concatenates the entire address fields.</description>
    <externalId>false</externalId>
    <formula>Address__c + BR() + City__c + &quot; &quot;+ TEXT(Province__c) + BR() + Postal_Code__c + BR() + TEXT(Country__c)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Concatenates the entire address fields.</inlineHelpText>
    <label>Location Address</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
