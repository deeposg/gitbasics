<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Order_Shipping_Address__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Connects the Order to the Account Address record to indicate the shipping address. If selected, overrides the written down fields.</description>
    <externalId>false</externalId>
    <inlineHelpText>Connects the Order to the Account Address record for the billing address. If selected, overrides the written down fields.</inlineHelpText>
    <label>Order Shipping Address</label>
    <referenceTo>Account_Address__c</referenceTo>
    <relationshipLabel>Quotes (Shipping Address)</relationshipLabel>
    <relationshipName>Orders1</relationshipName>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Lookup</type>
</CustomField>
